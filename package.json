{
  "name": "heavykatana",
  "private": true,
  "version": "0.1.0",
  "description": "HeavyKatana local dev server and bundling setup",
  "scripts": {
    "dev": "vite",
    "dev:lan": "vite --host 0.0.0.0 --port 8080 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 8080 --strictPort"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
